# Brew My Coffee

A small Next.js (App Router) starter for the Brew My Coffee site.

## Getting started

```bash
npm install
npm run dev
```

Then open [http://localhost:3000](http://localhost:3000).

## Deploy

This project is ready to deploy on [Vercel](https://vercel.com) — import the repo and it will build automatically.
